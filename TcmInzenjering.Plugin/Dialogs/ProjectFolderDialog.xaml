<Window x:Class="TcmInzenjering.Plugin.Dialogs.ProjectFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Folder projekta (TCM-INŽINJERING)"
        Height="200"
        Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
  <Grid Margin="14">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" TextWrapping="Wrap" Foreground="#333"
               Text="Izaberite folder u koji se snima projekat (3DFACE i ostali izlazi)."/>

    <DockPanel Grid.Row="1" Margin="0,12,0,0" LastChildFill="True">
      <Button DockPanel.Dock="Right" Content="Pregledaj..." Width="100" Height="28"
              Margin="8,0,0,0" Click="OnBrowse"/>
      <TextBox x:Name="PathBox" Height="28" VerticalContentAlignment="Center"/>
    </DockPanel>

    <Button Grid.Row="2" Content="Napravi nov folder" Width="160" Height="28"
            Margin="0,10,0,0" HorizontalAlignment="Left" Click="OnCreateFolder"/>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
      <Button Content="OK" Width="96" Height="28" IsDefault="True" Click="OnOk" Margin="0,0,8,0"/>
      <Button Content="Odustani" Width="96" Height="28" IsCancel="True" Click="OnCancel"/>
    </StackPanel>
  </Grid>
</Window>
