<Window x:Class="TcmInzenjering.Plugin.Dialogs.StationFontDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Font ispisa stacionaže"
        Height="220"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Izaberite font za oznake stacionaže (STA …)"
                   Margin="0,0,0,10"
                   TextWrapping="Wrap"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Font:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="FontBox"
                      Grid.Column="1"
                      Height="24"
                      IsEditable="True"
                      VerticalContentAlignment="Center"/>
        </Grid>

        <TextBlock Grid.Row="2"
                   x:Name="HintText"
                   FontSize="11"
                   Foreground="Gray"
                   TextWrapping="Wrap"
                   Text="Font se primenjuje pri sledećem crtanju ili osvežavanju stacionaža."/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="80" Height="26" Margin="0,0,8,0" IsDefault="True" Click="OnOk"/>
            <Button Content="Otkaži" Width="80" Height="26" IsCancel="True" Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
