<Window x:Class="TcmInzenjering.Plugin.Dialogs.ChainageFormatDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Parametar"
        Height="420"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="CROSS_CHAINAGE_TYPE&#x0a;Stacionaže na poprečnim osama"
                   TextAlignment="Center"
                   Margin="0,0,0,10"
                   FontWeight="SemiBold"/>

        <Grid Grid.Row="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="FormatNumberBox"
                     Height="24"
                     VerticalContentAlignment="Center"
                     MaxLength="2"
                     TextChanged="OnFormatNumberChanged"/>
            <Button Grid.Column="1"
                    Content="Početne vrednosti"
                    Height="24"
                    Margin="8,0,0,0"
                    Padding="8,0"
                    Click="OnDefaults"/>
        </Grid>

        <Border Grid.Row="2"
                Background="Black"
                BorderBrush="Gray"
                BorderThickness="1"
                Padding="8"
                Margin="0,0,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl x:Name="FormatList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="White"
                                       FontFamily="Consolas"
                                       FontSize="12"
                                       Margin="0,1"
                                       Text="{Binding DisplayText}"
                                       MouseLeftButtonUp="OnFormatItemClick"
                                       Cursor="Hand"
                                       Tag="{Binding Index}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="80"
                    Height="26"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OnOk"/>
            <Button Content="Cancel"
                    Width="80"
                    Height="26"
                    IsCancel="True"
                    Click="OnCancel"/>
        </StackPanel>
    </Grid>
</Window>
